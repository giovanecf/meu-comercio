export default class User {
  constructor(id, login, password, name, thumbnail, userId) {
    this.id = id;
    this.login = login;
    this.password = password;
    this.name = name;
    this.thumbnail = thumbnail;
    this.userId = userId;
  }
}
